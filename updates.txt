cmake bug
./anod (Fixed by downloading already built cmake of older style to sandbox, see changes to anod script)

termio bug
./src/cpp/Communications/ZeroMqZyreBridge.cpp
./src/cpp/Communications/LmcpObjectNetworkZeroMqZyerBridge.cpp

(Change termio to termios in the includes in these files... need to check for back compatibility with ubuntu 24)

uint32_t bug
./src/cpp/Utilities/UxAS_SentinelSerialBuffer.h (And many others)

Need to include <cstdint> in the include line (compilers let it slide before, now needs to be explicit)

AI Suggested:
grep -Rl "uint32_t" ./src/cpp \
  | xargs grep -L -E "#include <cstdint>|#include <stdint.h>" \
  | xargs sed -i '1i #include <cstdint>'

